# TaxMeter iOS App (SwiftUI)

## 실행 방법
1. `brew install xcodegen` (미설치 시)
2. `cd ios/TaxMeterApp`
3. `xcodegen generate`
4. 생성된 `TaxMeterApp.xcodeproj`를 Xcode에서 열고 실행

## 구성
- SwiftUI + MVVM
- Top 15 기준선 카드 입력/거리 계산
- `TaxMeterCore` 패키지 룰/거리 엔진 재사용
